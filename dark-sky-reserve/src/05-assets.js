'use strict';
/* Baked in by tools/build.py so the page is one file with nothing to
   fetch - it runs from a double-click, offline, with no local server. */
const SKY_IMAGE = '@@SKY_DATA_URI@@';
const SKY_LIGHT = @@SKY_LIGHT_JSON@@;
